…or push an existing repository from the command line
git remote add origin https://github.com/galvisjuanc/good-practices-c-.git
git branch -M main
git push -u origin main

Use git remote -v to see the remote branch connected
Change the remote to the version you are using
git remote set-url origin <github link>

The name of the project originally was good-practices-c#